# Dify RAG 知识库完整操作指南

> 基于 Dify 官方文档 (docs.dify.ai) 整理，版本覆盖 Dify Cloud / 社区版 v1.x
> 最后更新：2026 年 8 月

---

## 目录

1. [RAG 与 Dify 知识库概述](#1-rag-与-dify-知识库概述)
2. [创建知识库](#2-创建知识库)
3. [文档导入](#3-文档导入)
4. [分段（Chunking）配置](#4-分段chunking配置)
5. [索引方式选择](#5-索引方式选择)
6. [检索策略配置](#6-检索策略配置)
7. [Rerank 重排序模型](#7-rerank-重排序模型)
8. [元数据管理](#8-元数据管理)
9. [召回测试与验证](#9-召回测试与验证)
10. [集成到应用中](#10-集成到应用中)
11. [维护与持续优化](#11-维护与持续优化)
12. [最佳实践与常见问题](#12-最佳实践与常见问题)

---

## 1. RAG 与 Dify 知识库概述

### 1.1 什么是 RAG

RAG（Retrieval-Augmented Generation，检索增强生成）是一种将**信息检索**与**大模型生成**相结合的技术架构。核心流程：

```
用户提问 → 从知识库检索相关内容 → 将「问题 + 检索结果」发送给 LLM → LLM 基于资料生成答案
```

简单说就是：**先查资料，再回答问题**——让模型"开卷答题"而不是凭记忆瞎编。

### 1.2 Dify 知识库的核心概念

| 概念 | 说明 |
|------|------|
| **知识库（Knowledge Base）** | 整体容器，你的自有数据集合 |
| **文档（Document）** | 导入的单个文件或数据源 |
| **分段 / 块（Chunk）** | 文档切分后的可检索片段，是检索的基本单元 |
| **向量（Vector）** | 分段经 Embedding 模型转换后的数值化表示 |
| **召回（Retrieval）** | 根据用户问题从知识库中找到最相关的分段 |

### 1.3 知识库适用场景

| 场景 | 适合 Dify 知识库？ | 说明 |
|------|-------------------|------|
| 内部 FAQ、政策、SOP | ✅ 非常合适 | 文档结构清晰，更新周期可控 |
| 产品手册、API 文档 | ✅ 合适 | 按版本拆分知识库 |
| 客服话术、工单知识 | ✅ 合适 | 配合 Workflow 分支检索 |
| 实时库存、订单状态 | ❌ 不适合纯 RAG | 用 API 工具；知识库存放静态说明 |
| 需要强逻辑推理的证明题 | ⚠️ 有限 | RAG 提供证据，不保证推理正确 |

### 1.4 RAG 效果公式

```
RAG 效果 = 文档质量 × 解析质量 × 分段质量 × 检索质量 × 重排质量 × Prompt 组织 × 模型质量 × 评测闭环
```

> 这是一个**乘法关系**：任意一个环节特别差，整体效果就会被明显拉低。排查问题时不要只怪模型，从最上游的文档开始检查。

---

## 2. 创建知识库

### 2.1 创建入口

在 Dify 主界面导航栏点击 **知识库** → **创建知识库**。Dify 提供三种创建方式：

| 方式 | 说明 | 适合人群 |
|------|------|---------|
| **创建即用型知识库** | 导入数据 → 设置处理规则 → 一键完成 | 新手、快速上手 |
| **构建自定义知识库** | 通过知识流水线（Pipeline）自定义编排每个处理步骤 | 高级用户、复杂需求 |
| **连接外部知识库** | 通过 API 直接同步外部知识库 | 已有外部数据系统 |

### 2.2 即用型知识库创建步骤

```
步骤 1：点击「知识库」→「创建」→「创建即用型知识库」
步骤 2：上传文档（本地文件 / Notion / 网页导入 / 空知识库）
步骤 3：指定分段设置（分隔符、长度、重叠等）
步骤 4：选择索引方式和检索策略
步骤 5：等待数据处理完成 → 状态变为「可用」
```

---

## 3. 文档导入

### 3.1 支持的源数据格式

| 类型 | 格式 |
|------|------|
| **长文本** | TXT、Markdown、DOCX、HTML、JSON、PDF |
| **结构化数据** | CSV、Excel（XLSX/XLS） |
| **外部源** | Notion 页面、网页 URL |

### 3.2 导入前文档预处理建议

在导入 Dify 之前，应先清洁文档：

- 移除重复的页眉页脚、页码
- 清除导航菜单、水印
- 确保文档内标题层级结构清晰（H1 → H2 → H3）
- 图片中有文字内容的，确保 OCR 质量
- 表格应结构规整（合并单元格可能导致解析异常）

### 3.3 ETL 方案说明

| ETL 方案 | 适用版本 | 支持格式 |
|----------|---------|---------|
| **Dify ETL**（默认） | 社区版 | TXT, MD, PDF, HTML, XLSX, DOCX, CSV |
| **Unstructured ETL** | SaaS 版默认 / 社区版可选 | 额外支持 EML, MSG, PPTX, XML, EPUB |

> 社区版可通过环境变量开启 Unstructured ETL 以获得更强的文件解析能力。

---

## 4. 分段（Chunking）配置

分段是 RAG 中**最关键的调参点**。大模型上下文窗口有限，必须将长文本切分为小块后才能高效检索。

### 4.1 分段核心参数

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| **分隔符** | 切分文本的标识符 | `\n\n`（段落）或 `\n`（换行） |
| **分段最大长度** | 单个分段的最大字符数 | 500-1000 tokens（约 500-1000 中文字符） |
| **分段重叠长度** | 相邻分段间的重叠字符数 | 分段最大长度的 10%-20% |

### 4.2 两种分段模式

Dify 提供**通用模式**和**父子模式**两种分段方案。**创建知识库后无法切换**，需慎重选择。

#### 通用模式（General）

```
文档
 └── 分段 A（500 字）
 └── 分段 B（500 字，与 A 重叠 50 字）
 └── 分段 C（500 字，与 B 重叠 50 字）
```

- 所有分段共享相同的设置
- 匹配的分段直接返回
- 适合：FAQ、术语表、简单独立内容

#### 父子模式（Parent-Child）

```
文档
 └── 父分段 1（全文 / 大段落）
 │     ├── 子分段 1a（精确匹配用）
 │     ├── 子分段 1b
 │     └── 子分段 1c
 └── 父分段 2
       ├── 子分段 2a
       └── 子分段 2b
        
检索流程：在子分段中搜索 → 命中子分段 1b → 返回整个父分段 1
```

- **子分段**：较小片段，用于精确语义匹配
- **父分段**：较大的上下文片段，检索命中子分段后返回完整的父分段
- 解决"精确检索 vs 丰富上下文"的矛盾

| 维度 | 通用模式 | 父子模式 |
|------|---------|---------|
| 分段策略 | 单层，所有分段使用相同设置 | 双层，父/子分段分别设置 |
| 检索时返回 | 直接返回匹配的分段 | 返回匹配子分段所属的整个父分段 |
| 兼容索引方式 | 高质量、经济 | 仅高质量 |
| 最佳场景 | FAQ、术语表等独立内容 | 技术手册、研究论文等信息密集型文档 |

**父分段的两种创建模式**：

| 父分段模式 | 说明 | 适用场景 |
|-----------|------|---------|
| **段落** | 按分隔符和最大长度拆分为多个父分段 | 结构良好的长文档 |
| **全文** | 整个文档作为一个父分段（前 10,000 token 后截断） | 小型紧密关联的文档 |

> ⚠️ 注意：父子模式下，避免使用互为子集的分隔符（如 `??` 和 `?`），推荐使用 `??` 和 `##`。

### 4.3 文本预处理选项

在分段前可启用自动清洗：

| 预处理选项 | 效果 |
|-----------|------|
| **替换连续空格、换行符和制表符** | 3+ 连续换行 → 2 个换行；多空格 → 单空格 |
| **删除所有 URL 和电子邮件地址** | 移除链接噪音 |

### 4.4 预览分段

点击 **预览** 查看分段效果。如果结果不理想，调整参数后再预览。创建后也可以手动微调分段。

---

## 5. 索引方式选择

分段完成后，Dify 需要将文本转换为可检索的索引结构。

### 5.1 两种索引方式对比

| 特性 | 高质量（High Quality） | 经济（Economy） |
|------|----------------------|----------------|
| **原理** | Embedding 模型将文本转为向量 | 每个区块使用 10 个关键词 + 倒排索引 |
| **准确度** | 高（语义级匹配） | 中等（关键词匹配） |
| **Token 消耗** | 有（调用 Embedding 模型） | 无 Token 消耗 |
| **检索方式** | 向量检索 / 全文检索 / 混合检索 | 仅倒排索引 |
| **升降级** | 创建后不可降级为经济 | 可升级为高质量 |
| **适合场景** | 复杂问答、多语言、高精度需求 | 预算有限、简单 FAQ |

### 5.2 高质量索引原理

```
文本分段 → Embedding 模型（如 text-embedding-3-small）→ 多维向量 → 向量数据库
        
  "苹果很好吃"   →  [0.12, -0.34, 0.56, ...]
  "这个水果很甜" →  [0.11, -0.32, 0.58, ...]  ← 向量距离很近，语义相似
```

### 5.3 Q&A 模式（仅社区版）

系统自动为每个分段生成 Q&A 匹配对，采用 **Q to Q**（问题匹配问题）策略：

```
用户问："如何重置密码？"
系统匹配："Q: 如何重置密码？" → 返回对应答案分段
```

- 适用于 FAQ 类文档
- 仅支持中、英、日三语
- 会消耗额外 LLM Token
- 无法使用经济型索引

---

## 6. 检索策略配置

知识库收到用户查询后，按预设检索方式查找相关内容。检索策略直接影响 LLM 获取的上下文质量。

### 6.1 三种检索方式总览

| 模式 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **向量检索** | Embedding 语义相似度 | 能理解同义词、改写、跨语言 | 可能遗漏精确术语、SKU 编码 |
| **全文检索** | BM25 / 关键词匹配 | 精确匹配专有名词、编号、条款代号 | 对同义词和改写弱 |
| **混合检索** | 向量 + 关键词融合 | 综合两者优势，默认推荐 | 需调权重和 TopK |

### 6.2 向量检索

```
用户问题向量化 → 与知识库分段向量比较距离 → 返回最相邻的分段
```

**参数**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| **Rerank 模型** | 关闭 | 开启后用三方 Rerank 模型重新排序结果 |
| **TopK** | 3 | 返回相似度最高的前 K 个分段 |
| **Score 阈值** | 0.5 | 相似度分数下限，低于此分数的分段不被召回 |

> TopK 和 Score 阈值仅在 **开启 Rerank 模型后生效**。

### 6.3 全文检索

- 索引文档中所有词汇
- 用户输入问题后，通过关键词精确匹配
- 类似传统搜索引擎的检索方式

### 6.4 混合检索（推荐首选）

同时执行向量检索和全文检索，合并去重后排序。提供两种排序方式：

#### 权重设置（无额外 Token 成本）

| 权重配置 | 效果 |
|---------|------|
| **语义 = 1** | 纯向量语义检索，适合跨语言、同义词场景 |
| **关键词 = 1** | 纯关键词检索，适合精确术语/编号查询 |
| **自定义权重** | 灵活调节两者比例（如 0.7 语义 + 0.3 关键词） |

#### Rerank 模型（有额外成本但精度更高）

开启后使用外部 Rerank 模型对混合检索结果精排。

### 6.5 经济模式下的检索

仅支持**倒排索引** + **TopK** 设置，无 Score 阈值和 Rerank。

### 6.6 检索方式选择决策树

```
开始
 ├─ 预算极有限 / 内容简单？ → 经济模式
 ├─ 用户经常使用精确术语/编号？
 │   ├─ 是 → 混合检索，提高关键词权重
 │   └─ 否 ↓
 ├─ 多语言查询 / 同义词场景？
 │   ├─ 是 → 混合检索，提高语义权重
 │   └─ 否 ↓
 ├─ 追求最高精度，可接受额外成本？
 │   ├─ 是 → 混合检索 + Rerank 模型
 │   └─ 否 → 混合检索 + 权重设置（默认比例）
```

---

## 7. Rerank 重排序模型

### 7.1 为什么需要 Rerank

向量 TopK 按"语义距离"排序，不一定是"业务最相关"的排序。Rerank 模型对「问题 + 每条候选分段」进行交叉编码打分：

```
向量检索 Top-20 → Rerank 模型精排 → 返回 Top-3 最相关分段
```

- 延迟增加约 100-300ms
- 通常能显著提升最终送给 LLM 的上下文质量
- 典型 Rerank 模型：Cohere Rerank、bge-reranker、Jina Reranker

### 7.2 配置方法

1. 前往 **集成** → **模型供应商**
2. 配置 Rerank 模型的 API Key
3. 在知识库检索设置中启用 Rerank 模型

### 7.3 注意事项

- 若选择了多模态 Embedding 模型（带 Vision 图标），需同样选择多模态 Rerank 模型
- 重排序后仍建议发 3-5 个分段给 LLM，避免单个分段信息不完整

---

## 8. 元数据管理

元数据是"描述文档属性的数据"，用于实现**基于文档筛选的精准检索**。

### 8.1 元数据字段分类

| 类型 | 说明 | 可修改？ |
|------|------|---------|
| **内置元数据** | `document_name`、`uploader`、`upload_date`、`last_update_date`、`source` | 不可修改字段名和值 |
| **自定义元数据** | 用户自定义字段（字符串/数字/时间类型） | 可完全自定义 |

### 8.2 自定义元数据示例

建议事先规划好元数据体系：

```
字段示例：
  - product: "billing-system"
  - version: "v2.3"
  - department: "engineering"
  - doc_type: "policy | faq | runbook"
  - valid_until: "2026-12-31"
```

### 8.3 元数据操作

| 操作 | 位置 | 影响范围 |
|------|------|---------|
| 创建/修改/删除字段 | 知识库 → 元数据管理 | **全局**：影响知识库所有文档 |
| 批量编辑文档元数据 | 文档列表 → 勾选 → 元数据 | 所选文档 |
| 单篇编辑 | 文档详情 → 开始标记 | 仅该文档 |

### 8.4 在检索中使用元数据筛选

在知识检索节点或聊天助手设置中，通过筛选条件使用元数据：

```
# 字符串字段支持的条件：
is / is not / contains / not contains / starts with / ends with / is empty / is not empty

# 数字字段：= ≠ > < ≥ ≤ is empty / is not empty
# 时间字段：is / before / after / is empty / is not empty
```

筛选值可来自变量或常量。条件间支持 AND / OR 逻辑。

---

## 9. 召回测试与验证

### 9.1 在知识库内直接测试

路径：知识库 → 左侧边栏 **检索测试** 图标

- 模拟用户提问，测试召回效果
- 可临时调整检索参数（**仅对当前测试会话生效**）
- 查看具体命中了哪些分段及相似度分数

### 9.2 记录（Logs）

**记录**板块保存该知识库的所有检索事件：

- 召回测试页面的测试检索
- 关联应用发起的常规检索（测试和生产环境）

### 9.3 构建离线评估集

对于生产级应用，建议构建评估体系：

1. 准备 30-100 个真实用户问题
2. 为每个问题标注：
   - **期望的答案要点**
   - **应命中的文档/段落**
   - **是否允许拒绝回答**
3. 追踪指标：

| 指标 | 含义 |
|------|------|
| **Recall@K** | 标准答案文档是否出现在 TopK 中 |
| **Answer Faithfulness** | 答案是否被召回分段支撑 |
| **拒绝率** | 无知识时是否正确拒绝而非编造 |

### 9.4 效果排查流程图

```
RAG 效果不好
  ├─ 完全找不到资料？→ 查入库链路：文档是否上传、解析是否成功、Embedding 是否一致
  ├─ 召回内容答非所问？→ 查检索策略：检索方式、TopK、阈值、Rerank
  ├─ 资料找到了但模型乱答？→ 查 Prompt 约束：是否限制"仅基于资料回答"
  ├─ 答案不完整或漏重点？→ 查分段：Chunk 大小、Overlap、TopK、上下文窗口
  └─ 无法追溯来源？→ 查引用：是否开启引用和归属功能
```

---

## 10. 集成到应用中

### 10.1 聊天助手快速集成

```
1. 工作室 → 创建应用 → 聊天助手
2. 上下文设置 → 添加 → 选择已创建的知识库
3. 参数设置 → 配置召回策略
4. 元数据筛选 → 配置筛选条件（可选）
5. 添加功能 → 开启「引用和归属」
6. 调试与预览 → 测试效果
7. 保存并发布
```

### 10.2 Workflow / Chatflow 集成

在 Workflow 中使用 **知识检索节点**：

- 可绑定多个知识库
- 支持元数据筛选（禁用/自动/手动三种模式）
- 检索结果通过变量传递给 LLM 节点的上下文

### 10.3 Prompt 约束模板（推荐）

```
你是内部知识库助手。严格遵守以下规则：
1. 只根据 [检索到的上下文] 中的内容回答问题，不要使用训练数据中的知识猜测。
2. 如果上下文信息不足，回复："根据当前知识库资料，我无法确认此问题。请联系 XX 部门或提交工单。"
3. 尽可能引用来源（文档标题或段落）。
4. 不要编造政策条款、API 参数、价格或日期。
5. 对于安全、合规或财务相关操作，提醒用户遵循官方最新文档。

[检索到的上下文]
{{knowledge_retrieval.result}}
```

建议 Temperature 设置为 **0.1-0.5**（FAQ 类应用应偏低）。

### 10.4 多知识库场景

- **多路召回**：应用可关联多个知识库 → 在所有库中并行检索 → 合并结果 → Rerank
- **引用和归属**：开启后可显示答案来源于哪个知识库/文档
- **注意**：多个知识库使用不同的 Embedding 模型时，无法手动调整权重 → 需统一模型或启用 Rerank

### 10.5 查询改写（高级技巧）

在 Workflow 中加入 LLM 节点进行查询改写：

| 技术 | 说明 |
|------|------|
| **HyDE** | 先生成假想答案再检索（探索性问题，合规场景慎用） |
| **多查询扩展** | 生成 2-3 个改写查询，并行检索，合并去重 |
| **指代消解** | 结合历史将"它"、"刚才说的"展开为完整表达 |

---

## 11. 维护与持续优化

### 11.1 文档管理

| 操作 | 说明 |
|------|------|
| **添加** | 导入新文档 |
| **修改分段设置** | 调整分段规则（**分段结构不可改**） |
| **删除** | 永久删除文档，不可撤销 |
| **启用/禁用** | 临时纳入或排除检索 |
| **归档/取消归档** | 归档后只读不参与检索 |

> Dify Cloud 自动禁用机制：Sandbox 计划 7 天未活跃自动禁用，Professional/Team 计划 30 天。

### 11.2 分段管理

| 操作 | 说明 |
|------|------|
| **查看分段** | 按文档筛选或搜索分段内容 |
| **编辑分段** | 修正解析异常的内容 |
| **新增分段** | 手动补充遗漏信息 |
| **删除分段** | 移除错误或重复分段 |
| **添加摘要** | 为分段添加摘要（摘要同样被向量化索引） |
| **添加关键词** | 仅经济索引模式可用（最多 10 个） |

### 11.3 父子模式的最佳实践

**将子分段用作检索钩子**：编辑子分段（不影响父分段），将其改写为更好的检索标签：

- **关键词式**："闪烁灯、无法开机、红灯、连接错误、卡死"
- **摘要式**："LED 颜色解读及硬件电源或配对问题排查指南"
- **问题式**："常亮红灯是什么意思？"

### 11.4 使用摘要弥合查询与内容差距

当原始分段过于具体或包含噪音时，通过摘要提供精简的语义层：

- 查询语言与文档语言不同 → 按用户提问方式添加摘要
- 概念隐含在细节中 → 提炼高层次摘要
- 原始文本为代码/表格/日志 → 添加描述性摘要
- 需要**分组检索** → 多个分段使用相同摘要，语义"粘合"（受 Top K 限制）

### 11.5 知识库设置可随时调整

路径：知识库 → 左侧边栏 **设置** 图标

| 设置项 | 说明 |
|--------|------|
| 名称和图标 | 标识和区分知识库 |
| 权限 | 定义哪些工作空间成员可访问 |
| 索引方式 | 经济 → 高质量（可升级不可降级） |
| 嵌入模型 | 更换后**所有分段重新向量化** |
| 摘要自动生成 | 仅对新添加文档生效 |
| 检索设置 | 调整检索方式和参数 |

---

## 12. 最佳实践与常见问题

### 12.1 分段大小建议

| 文档类型 | 推荐分段大小 | 推荐重叠 |
|---------|-------------|---------|
| FAQ | 200-400 字 | 50 字 |
| 技术手册 | 500-800 字 | 80-100 字 |
| 法律/政策文档 | 400-600 字 | 60-80 字 |
| 研究论文 | 600-1000 字 | 100-150 字 |

### 12.2 检索参数调优策略

| 问题现象 | 可能原因 | 调优方向 |
|---------|---------|---------|
| 找不到相关内容 | Score 阈值过高 | 降低阈值（如 0.5 → 0.3） |
| 召回内容太多太杂 | TopK 太大 / 阈值过低 | 减小 TopK / 提高阈值 / 启用 Rerank |
| 答案答非所问 | 分段过大含噪音 | 减小分段 / 启用 Rerank |
| 缺少关键信息 | 分段过小 / TopK 过小 | 增大分段 / 提高 TopK / 使用父子模式 |
| 专有名词搜不到 | 纯向量检索 | 切换混合检索，提高关键词权重 |

### 12.3 常见问题速查（QA）

**Q: 分段模式创建后能否修改？**
A: 不能。通用模式和父子模式在知识库创建后不可切换。但分隔符、分段最大长度等细节参数可以随时调整。

**Q: 高质量索引能否降级为经济索引？**
A: 不能。经济索引可以在设置中升级为高质量，反之不行。

**Q: 多个知识库能否绑定到一个应用？**
A: 可以。多路并行检索，注意总 Token 限制。在 Workflow 中可按类别分流到不同知识库。

**Q: 为什么我的 PDF 表格解析效果差？**
A: PDF 表格是 RAG 的常见难点。建议：将表格导出为 Markdown 格式、手动整理后再导入，或使用知识流水线自定义解析。

**Q: 如何防止员工上传敏感文档？**
A: 流程上导入审批 + 敏感词扫描。技术上：权限隔离知识库、导入审计日志。

**Q: 多久评测一次？**
A: 大量文档更新或检索参数变更后，立即跑离线评估集。线上至少每月审核"无帮助"标记，旺季每周一次。

**Q: 知识库更新后需要重建索引吗？**
A: 普通文档增删改实时生效。更换 Embedding 模型会触发全量重新向量化。大批量更新时注意队列和磁盘状态。

### 12.4 推荐排查顺序

```
文档内容 → 分段设置 → 检索参数 → Prompt 约束 → 模型选择
```

> 不要一上来就换最大的模型。绝大多数问题出在前面的数据处理和检索环节。

---

## 附录：Dify 知识库操作快速参考

### A. 创建到使用全流程

```
创建知识库 → 导入文档 → 分段设置 → 索引方式 → 检索策略 → 嵌入完成
                                                              ↓
创建应用 → 上下文绑定知识库 → 配置召回参数 → 调试测试 → 发布上线
                                                              ↓
                                              监控日志 → 维护文档 → 召回测试 → 持续优化
```

### B. 关键参数速记

| 参数 | 默认值 | 含义 |
|------|--------|------|
| 分段最大长度 | 500 | 单分段字符上限 |
| 分段重叠长度 | 50 | 相邻分段重叠字符数 |
| TopK | 3 | 返回相似度最高的前 K 个分段 |
| Score 阈值 | 0.5 | 相似度分数下限 |

### C. 两种分段模式速记

| | 通用模式 | 父子模式 |
|------|---------|---------|
| 分段层数 | 1 层 | 2 层 |
| 检索返回 | 匹配的分段 | 匹配子分段 → 返回父分段 |
| 索引方式 | 高质量 + 经济 | 仅高质量 |
| 适合场景 | 简单独立内容 | 信息密集型长文档 |

---

> 本文档基于 Dify 官方文档 (docs.dify.ai) 及社区最佳实践系统整理。
> 更多细节请参阅：
> - [Dify 知识库官方文档](https://docs.dify.ai/zh/use-dify/knowledge/readme)
> - [Dify 社区论坛](https://forum.dify.ai/)
